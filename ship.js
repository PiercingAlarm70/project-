var shipImg1
var shipImg2
var sea

var ship = createsprite = ship 

ship = Loadanimation = shipImg1, shipImg2

function preload(){

  var shipImg1 = Loadanimation = ship-1.png
var shipImg2 = Loadanimation = ship-2.png
var sea = Loadanimation = sea.png

}

function setup(){
  createCanvas(400,400);
  
}

function draw() {
  background("blue");
  if( background.x < 0){ background.x = background.width/8; }
}